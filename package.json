{
  "name": "atmosbyte-build-tools",
  "version": "1.0.0",
  "description": "Build tools for Atmosbyte project",
  "private": true,
  "scripts": {
    "minify": "html-minifier-terser --input-dir web/templates --output-dir web/templates-min --file-ext html --collapse-whitespace --remove-comments --remove-optional-tags --remove-redundant-attributes --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype --minify-css true --minify-js true",
    "clean": "rimraf web/templates-min",
    "watch": "chokidar 'web/templates/**/*.html' -c 'npm run minify'"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "rimraf": "^5.0.5",
    "chokidar-cli": "^3.0.0"
  }
}
